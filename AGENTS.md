- high-level planning is usually done by Claude Code, we use Codex to do the concrete implementation.

- After ANY correction from the user, update `tasks/cc_lessons.md` with the pattern so we could reuse in the future.

Core principles:
- Simplicity first: make every change as simple as possible. Impact minimal code.